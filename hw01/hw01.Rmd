---
title: "Домашняя работа №1"
author: "Dmitrii Lokshtein"
date: '5 октября 2018 г '
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Работа с данными

Загрузили данные в датафрейм при помощи read.table
```{r}
data.df<-read.table("http://people.math.umass.edu/~anna/Stat597AFall2016/rnf6080.dat")
```
Сколько строк и столбцов в data.df? Если получилось не 5070 наблюдений 27 переменных, то проверяйте аргументы.
Строк:`r nrow(data.df)`
Столбцов:`r ncol(data.df)`


```{r, echo=FALSE}
colnames(data.df) # имена колонок из data.df
data.df[5,7] #значение из 5 строки седьмого столбца
data.df[2,] # 2 строка из data.df
names(data.df) <- c("year", "month", "day", seq(0,23)) #данная строка перезадает имена колонок
head(data.df) #помогает отобразить голову списка и задать ее длину
tail(data.df) #помогает отобразить хвост списка и задать его длину
data.df$daily<-apply(data.df[,4:27], 1, sum)# добавили колонку daily
hist(data.df$daily, prob = TRUE, main = "Гистограмма частоты количества осадков",
     sub = "Количество осадков за день")
```
Вывод: Имеются отрицательные значения

Исправили data.df
```{r}
fixed.df<-data.df
for (i in 1:nrow(fixed.df)){
  for (j in 1:ncol(fixed.df))
  {
    if (data.df[i,j] < 0)
    {
      data.df[i,j]<-0
    }
  }
}
names(fixed.df) <- c("year", "month", "day", seq(0,23), "daily")
fixed.df$daily<-apply(data.df[,4:27], 1, sum)

hist(fixed.df$daily, prob = TRUE, main = "Гистограмма частоты количества осадков",
     sub = "Количество осадков за день")
```

## Синтаксис и типизирование

v <- c("4", "8", "15", "16", "23", "42") - создает вектор значений
max(v) - ищет максимум (символ "8" элемента вектора "8" имеет максимальное значение в кодировке) 
sort(v) - сортирует элементы  по возрастанию
sum(v) - Ошибка выполнения функции, т.к. функция не может работать с типом аргумента - "символ", вектор v состоит из символов

v2 <- c("5",7,12)
v2[2] + 2[3] - Не может сложить числа. Ошибка: unexpected symbol in "v2[2] + 2[3] - не может"

df3 <- data.frame(z1="5",z2=7,z3=12)
df3[1,2] + df3[1,3] - складывает 2 и 3 элемент датафрейма

l4 <- list(z1="6", z2=42, z3="49", z4=126)
l4[[2]] + l4[[4]] - склдадывает значения элементов 2 и 4 компонентов списка
l4[2] + l4[4] - Не может сложить z2 и z4 компоненты списка Ошибка: Error in l4[2] + l4[4] : non-numeric argument to binary operator
 
##Работа с функциями и операторами
1.Оператор двоеточие создаёт последовательность целых чисел по порядку. Этот оператор — частный случай функции seq(), которую вы использовали раньше. Изучите эту функцию, вызвав команду ?seq. Испольуя полученные знания выведите на экран

i. Числа от 1 до 10000 с инкрементом 372.
`r seq(1, 10000, 372)`

ii. Числа от 1 до 10000 длиной 50.
`r seq(1, 10000, length.out = 50)`

2.Функция rep() повторяет переданный вектор указанное число раз. Объясните разницу между rep(1:5,times=3) и rep(1:5, each=3)

```{r}
rep(1:5,times=3)
rep(1:5, each=3)
```
Ответ: В первом случае дублируется весь вектор. Во втором каждое число




